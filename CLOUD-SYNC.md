# 专属链接云端账本

在有账单的电脑上打开首页。如果尚未启用云端，点击「启用云端同步并生成专属链接」，将已保存的账单上传一次。已连接云端的设备无需再次上传。

点击「复制专属链接 · 发到手机」。所有设备打开同一个完整链接，即自动连接同一账本，无需输入连接码或点击连接按钮。可收藏完整链接。持有链接的人拥有账本读写权限，请私下分享。

专属凭据存放在 URL 的 fragment（# 后），不提交到静态页面服务器，也不写入公开仓库。连接通过 HTTPS Authorization 头访问 Cloudflare D1 后端。

电脑与手机双向同步：保存成功后显示「已同步到云端」，空闲页面约每 8 秒读取更新。正在填写时保留当前输入。并发写入通过版本号保护，发生冲突时保留输入，复制后读取云端最新再重试。

原本地账本保留；已存在的云端账本不会自动与其他设备的本地账单合并。断开连接不删除云端数据。后端位于 travel 仓库，D1 绑定 LEDGER_DB。
