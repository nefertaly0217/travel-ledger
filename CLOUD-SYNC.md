# 多设备共享账本

1. 在有账单的电脑浏览器打开首页，点击「上传已有账单并创建共享账本」。此操作上传已保存的账单与成员，保留原本地账本。
2. 展开「连接其他设备 / 管理连接」，复制连接码，私下传到手机。
3. 手机打开相同网址，粘贴连接码，点击「连接共享账本」。不要在手机再次创建新账本。

后续编辑保存到 Cloudflare D1。空闲页面每 8 秒读取云端；编辑中不自动替换输入。保存失败时页面明确提示，未确认成功的操作需要重试。并发修改通过版本号拒绝覆盖；保留未保存输入后读取最新版本再编辑。

连接码具有读写权限，请妥善保存，不要放进公开链接、GitHub 或截图。断开连接不删除云端账本。原有本地备份不自动与已存在的云端账本合并。

后端在 travel 仓库的 ledger-sync-worker.mjs，D1 绑定 LEDGER_DB。自动部署配置首次创建数据库后，维护者可将 Cloudflare 显示的数据库 ID 固定到 wrangler.jsonc。
